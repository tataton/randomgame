Changes made on branch andyAdds
-------------------------------

* Added some comments.
* Changed name "getMax" to "postMax" (because that's what it does).
* Function rand simplified to return value directly instead of creating intermediate variable.
